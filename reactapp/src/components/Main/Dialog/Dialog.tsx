import React, { MouseEventHandler } from "react";
import { StyledDialog } from "./Dialog.style.js";

interface Iprops {}
function Dialog(props: Iprops) {
  return <StyledDialog>Dialog</StyledDialog>;
}

export default Dialog;
